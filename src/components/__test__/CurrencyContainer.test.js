import '@testing-library/jest-dom'
import { render } from '@testing-library/react'
import CurrencyContainer from '../CurrencyContainer'

test('Rendered currency container', () => {
  render(<CurrencyContainer />)
})
